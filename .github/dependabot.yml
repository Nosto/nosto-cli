version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
    commit-message:
      prefix: "deps"
    open-pull-requests-limit: 10
    allow:
      - dependency-type: "all"
    groups:
      minor-and-patch-updates:
        update-types:
          - "minor"
          - "patch"
      eslint:
        patterns:
          - "@eslint/*"
          - "eslint"
          - "eslint-*"
          - "typescript-eslint"
      vitest:
        patterns:
          - "@vitest/*"
          - "vitest"
      commitlint:
        patterns:
          - "@commitlint/*"
          - "commitlint"